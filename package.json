{
  "name": "invoice-tracker",
  "version": "1.0.0",
  "private": true,
  "description": "App de facturación full-stack (React + Vite + Express + SQLite).",
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,green \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "node backend/server.js",
    "dev:frontend": "npm --prefix invoice-app run dev",
    "install:all": "npm install && npm --prefix backend install && npm --prefix invoice-app install"
  },
  "devDependencies": {
    "autoprefixer": "^10.5.0",
    "concurrently": "^9.1.0",
    "postcss": "^8.5.10",
    "tailwindcss": "^4.2.4"
  }
}
